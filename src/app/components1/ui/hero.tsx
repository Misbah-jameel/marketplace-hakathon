// ParentComponent.tsx or any other file where HeroSection is used
import HeroSection from "@/app/components/HeroSection"; // Ensure the correct path to HeroSection file

export default function SomeComponent() {
  return (
    <div>
      <HeroSection />
    </div>
  );
}
